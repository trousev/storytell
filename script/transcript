#!/bin/bash
set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPT_DIR")"

cd "$PROJECT_DIR"

export CUDA_HOME=/usr/local/cuda
export PATH=$CUDA_HOME/bin:$PATH

exec .venv/bin/python -W ignore::UserWarning script/transcript_impl.py "$@"